Each module is self-contained with its lifecycle (`init`, `run`, `destroy`).

## ⚙️ Technologies
- JavaScript / TypeScript  
- Vite / Webpack  
- WebSocket for live interaction  
- Optional AI sync with Aura Brain API
